# Copy this file to .env (or export these in your shell) to enable
# LLM-generated reviewer summaries instead of the template fallback.
# Only one is needed. Checked in this order: Groq, OpenAI, Anthropic.

GROQ_API_KEY=
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
